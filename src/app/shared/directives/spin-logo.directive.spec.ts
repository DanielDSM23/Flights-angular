import { SpinLogoDirective } from './spin-logo.directive';

describe('SpinLogoDirective', () => {
  it('should create an instance', () => {
    const directive = new SpinLogoDirective();
    expect(directive).toBeTruthy();
  });
});
